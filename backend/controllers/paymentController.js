'use strict';
const paymentService=require('../services/paymentService');
const {MESSAGES,ROLES}=require('../config/constants');
async function payFee(req,res){try{const feeId=parseInt(req.params.id,10);if(!feeId||feeId<1)return res.status(400).json({success:false,message:'Invalid fee ID.'});const result=await paymentService.payFee(feeId,req.user);return res.status(200).json({success:true,message:'Payment processed successfully.',...result});}catch(err){const status=err.statusCode||500;if(status===500)console.error('[paymentController.payFee]',err.message);return res.status(status).json({success:false,message:status===500?MESSAGES.SERVER_ERROR:err.message});}}
async function getReceipt(req,res){try{const feeModel=require('../models/feeModel');const transactionModel=require('../models/transactionModel');const {generateReceipt}=require('../services/receiptService');const studentModel=require('../models/studentModel');const feeId=parseInt(req.params.id,10);const feeRecord=await feeModel.findByIdWithStudent(feeId);if(!feeRecord)return res.status(404).json({success:false,message:'Fee record not found.'});if(req.user.role===ROLES.ADMIN){if(feeRecord.school_id!==req.user.school_id)return res.status(403).json({success:false,message:'Access denied.'});}else{const student=await studentModel.findByUserId(req.user.id);if(!student||student.id!==feeRecord.student_id)return res.status(403).json({success:false,message:'Access denied.'});}const allTx=await transactionModel.findByStudent(feeRecord.student_id);const paidTx=allTx.find(tx=>tx.status==='paid'&&tx.memo_ref&&tx.memo_ref.includes(`FEE-${feeId}-`));if(!paidTx)return res.status(404).json({success:false,message:'No paid transaction found for this fee record.'});return res.status(200).json({success:true,...generateReceipt(paidTx,feeRecord)});}catch(err){console.error('[paymentController.getReceipt]',err.message);return res.status(500).json({success:false,message:MESSAGES.SERVER_ERROR});}}
module.exports={payFee,getReceipt};
